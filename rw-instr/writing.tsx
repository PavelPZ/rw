import React from 'react'; import course, {Page} from 'rw-course'; import { $l } from 'rw-lib/loc'; import l from './writing-loc'; export default () => /*
*********** START MARKUP HERE: */
<Page title="">
  <h1 className="techInstr  ">{$l(l.th381)}</h1>
  <p>{$l(l.tp381)}</p>
  <ol>
    <li>{$l(l.tli381)}</li>
    <li>{$l(l.tli382)}</li>
  </ol>
</Page>