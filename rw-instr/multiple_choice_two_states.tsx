import React from 'react'; import course, {Page} from 'rw-course'; import { $l } from 'rw-lib/loc'; import l from './multiple_choice_two_states-loc';   export default () => /*
*********** START MARKUP HERE: */
<Page title="">
  <h1 className="techInstr  ">{$l(l.th19)}</h1>
  <p>{$l(l.tp11)}</p>
  <ol>
    <li>{$l(l.tli30)}</li>
    <li>{$l(l.tli31)}</li>
  </ol>
  <p>{$l(l.tp12)}</p>
</Page>