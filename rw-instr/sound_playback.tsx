import React from 'react'; import course, {Page} from 'rw-course'; import { $l } from 'rw-lib/loc'; import l from './sound_playback-loc'; export default () => /*
*********** START MARKUP HERE: */
<Page title="">
  <h1 className="techInstr  ">{$l(l.th122)}</h1>
  <p>{$l(l.tp31)}</p>
  <ol>
    <li>{$l(l.tli61)}</li>
    <li>{$l(l.tli62)}</li>
    <li>{$l(l.tli63)}</li>
  </ol>
</Page>