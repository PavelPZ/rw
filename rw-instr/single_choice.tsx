import React from 'react'; import course, {Page} from 'rw-course'; import { $l } from 'rw-lib/loc'; import l from './single_choice-loc'; export default () => /*
*********** START MARKUP HERE: */
<Page title="">
  <h1 className="techInstr  ">{$l(l.th16)}</h1>
  <p>{$l(l.tp6)}</p>
  <ol>
    <li>{$l(l.tli24)}</li>
    <li>{$l(l.tli25)}</li>
  </ol>
</Page>