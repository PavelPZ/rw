import React from 'react'; import course, {Page} from 'rw-course'; import { $l } from 'rw-lib/loc'; import l from './dictation-loc';   export default () => /*
*********** START MARKUP HERE: */
<Page title="">
  <h1 className="techInstr  ">{$l(l.th118)}</h1>
  <p>{$l(l.tp27)}</p>
  <ol>
    <li>{$l(l.tli56)}</li>
    <li>{$l(l.tli57)}</li>
    <li>{$l(l.tli58)}</li>
  </ol>
  <p>{$l(l.tp28)}</p>
</Page>