import { toGlobId } from 'rw-lib/loc'; declare const __moduleName: string; const globId = toGlobId(__moduleName);
const th301: DLoc.ILocItem = {
  'en-gb': 'ODD ONE OUT',
};
const tp301: DLoc.ILocItem = {
  'en-gb': 'Your task is to mark the word that does not belong to the group. Proceed as follows:',
};
const tli301: DLoc.ILocItem = {
  'en-gb': 'Left click the selected word.',
};
const tli302: DLoc.ILocItem = {
  'en-gb': 'If you wish to change your selection, click another word.',
};
const res = { [globId]: { th301, tp301, tli301, tli302 } };
export default res;